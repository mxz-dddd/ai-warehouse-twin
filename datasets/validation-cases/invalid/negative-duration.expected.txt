输入校验未通过,发现 2 处问题:

  1. 缺少必填字段:场景 ID(scenario_id)。
     位置:each_pick.scenario_id

  2. 拣选作业时长(pick_service_duration_ms,毫秒) 不能为负数(当前值:-5)。
     位置:each_pick.process.pick_service_duration_ms

请按上面每条的“位置”修改后重新校验。
